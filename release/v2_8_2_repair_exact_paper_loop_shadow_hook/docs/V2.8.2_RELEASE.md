# V2.8.2 Repair Exact Paper Loop Shadow Hook

## Failure repaired
V2.8.1 inserted the characters `\n` before `def run(...)` instead of a real newline, producing a Python SyntaxError.

## Repair sequence
1. Restore `paper_daily_session/runner.py` from the V2.8.1 pre-patch backup.
2. Confirm the original method/call anchors exist.
3. Re-apply the same isolated V2.7 shadow hook using real newline characters.
4. Reject the result if the literal `\n    def run` sequence remains.
5. Compile the patched runner before allowing success.
6. Verify exactly one shadow method and one shadow call exist.

## Safety
The actual Paper loop is reused. No new loop, broker write, order submission, production selector change, or strategy parameter change is added.
