# Release Notes

V114.01–V115.00 introduces the Paper session scheduler foundation.

The scheduler manages market-session lifecycle and persisted restart state without contacting Alpaca or submitting orders. The next stage will connect these scheduler actions to the end-to-end Paper runtime.
