# AI Trading Engine V2.2.11 — ML Shadow Inference Bridge

Base commit: `0298d090`.

Purpose:
- reuse the exact V2.2.8.1 feature engineering path;
- load the frozen V2.2.10 selected 5m/15m/30m/60m models;
- verify each model SHA-256 before loading;
- generate local research-only ML predictions from historical + live shadow bars;
- rank the best V2.2.10 shadow-research horizon when available.

Explicitly not changed:
- canonical execution selector;
- confidence / reward-risk thresholds;
- Alpaca Paper execution;
- broker network trading endpoints;
- Paper or Live orders;
- model promotion.

Runtime output:
`runtime/ai_ml_shadow_inference_v2_2_11/`

Files:
- `latest_ml_shadow_inference.json`
- `ml_shadow_inference_ledger.jsonl`

This stage bridges trained ML artifacts into shadow inference without creating
another Champion/Challenger, registry, or promotion system.
