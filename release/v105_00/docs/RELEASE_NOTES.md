# Release Notes

V104.01–V105.00 adds broker-independent order intents and deterministic position sizing.

The output topic is `order.intent`. Broker execution remains disabled and no broker transport is included in the standard pipeline.
