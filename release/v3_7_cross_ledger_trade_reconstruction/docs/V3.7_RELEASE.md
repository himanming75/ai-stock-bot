# V3.7 Cross-Ledger Trade Reconstruction

V3.6 proved the five CLOSED_TRADE records contain no numeric P/L. V3.7 correlates them with separately stored actual fill/execution records.

Only explicit fill evidence with numeric price, quantity, side and timestamp is accepted. Exit matching uses identifier overlap first; a conservative symbol + quantity + close-time match is allowed only when strong. A prior opposite-side fill is then required. Ambiguous cases remain UNRESOLVED.

LONG: `(exit-entry)*qty`; SHORT: `(entry-exit)*qty`. Only explicitly stored fees are subtracted. No market-price, spread, slippage, or missing-fee estimates are used.

Audit: `runtime/dashboard_cross_ledger_v3_7/latest_cross_ledger_reconstruction.json`

Valid successful outcomes: `PASS_RECONSTRUCTED` or `PASS_DIAGNOSTIC_NO_SAFE_MATCH`.
