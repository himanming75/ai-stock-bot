# Release Notes

V111.01–V112.00 adds a tightly limited Alpaca Paper single-order opt-in.

The default runner remains offline. The separate actual runner can submit one Paper order only after explicit environment opt-in and preflight checks. Live Trading remains unreachable because the existing client locks the base URL to `paper-api.alpaca.markets`.
