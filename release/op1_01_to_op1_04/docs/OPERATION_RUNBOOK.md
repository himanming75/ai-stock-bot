# Read-Only Pilot Runbook

1. Confirm V143 final package is genuinely ready.
2. Store Paper credentials only in Windows user environment variables.
3. Run local tests first.
4. Copy and review input examples.
5. Run local snapshot preflight.
6. Run `-EnableNetwork` only for read-only account verification.
7. Review the result; do not enable orders.
