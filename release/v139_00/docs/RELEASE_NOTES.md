# Release Notes

V138.01–V139.00 joins readiness, cycle-token, preview, and final-approval gates into one resumable local continuation pipeline.

The current existing AAPL order remains active, so the expected state is WAIT_ACTIVE_ORDER.
