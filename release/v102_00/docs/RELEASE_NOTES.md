# Release Notes

V101.01–V102.00 replaces certificate-only progression with a real reusable runtime package.

The runtime remains single-threaded and explicitly tick-driven so its state transitions can be tested and replayed deterministically.
