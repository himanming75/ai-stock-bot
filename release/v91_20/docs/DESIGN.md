# V91.01-V91.20 Design

Adds manual opt-in, two independent approvals, 300-second single-use session tokens, revocation, kill switch, read-only permission gate, and audit events.

Scheduler dispatch, runtime auto-start, broker writes, and all order submission remain disabled.
