# V3.6 Trade Ledger Normalization & Performance Data Recovery

V3.5 observed five CLOSED_TRADE records but recovered zero numeric P/L values because it inspected only top-level fields.

V3.6:
- recursively scans nested CLOSED_TRADE payload/details/result structures
- prioritizes explicit realized-P/L keys over generic P/L keys
- records the exact field path used for each recovered value
- recursively normalizes symbol, quantity, reason, IDs and timestamps
- never invents or price-derives P/L
- never rewrites runtime source ledgers

Recovery audit:
`runtime/dashboard_trade_recovery_v3_6/latest_trade_recovery_audit.json`

The report includes observed trade count, recovered numeric P/L count, missing count, recovery rate, path/key counts and provenance samples.

Direct runtime verification starts the actual dashboard direct script on localhost:8876 and confirms V3.4 visualization still PASS and analytics normalization is consistent.

Safety: read-only source audit only; no broker network/write, no order submission, no Paper runtime modification, no production parameter/selector change, no duplicate engine.
