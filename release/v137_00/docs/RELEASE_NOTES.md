# Release Notes

V136.01–V137.00 adds the final local order-preview package and approval gate.

The current expected state is WAIT_CYCLE_TOKEN because the existing AAPL order remains active and V136 did not create a cycle token.
