# Release Notes

V126.01–V127.00 adds the first real Alpaca Paper POST pathway with strict opt-in and preflight controls.

Because the account currently contains one open recovered Legacy Bot order, the stage is expected to validate `EXISTING_ORDER_WAIT` and submit zero new orders. The next phase tracks the existing order through accepted, partial-fill, filled, canceled, rejected, or expired states.
