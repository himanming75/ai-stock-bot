# Release Notes

V120.01–V121.00 adds a GET-only actual Alpaca Paper read session for the autonomous runtime.

The session produces a redacted account snapshot with market state, balances, held symbols, and order counts. The default validation is offline; an optional actual Paper runner is provided behind explicit read-only opt-in.
