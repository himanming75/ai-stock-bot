# Release Notes

V117.01–V118.00 produces Continuous Paper Runtime RC1.

The runtime executes scheduler decisions continuously, sends cycle work through the operational stability layer, coordinates heartbeat and watchdog checks, performs controlled recovery, stops at session close, and supports clean restart after shutdown.
