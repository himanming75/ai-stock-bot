# V3.8.1 Canonical Lifecycle Patch Repair

V3.8 failed before modifying the analytics module because its patcher searched for a multiline V3.7 reconstruction marker that did not match current main.

Current main uses a compact V3.7 block:
- `rows.sort(...)`
- direct `import importlib.util`
- compact reconstruction module loading

V3.8.1 patches against the exact current-main structure.

It preserves V3.7 cross-ledger diagnostics and adds:
- canonical `paper_full_auto_lifecycle/closed_round_trips.jsonl` loading
- canonical-source priority for performance analytics
- lifecycle discovery/status exposure

No runtime or broker execution logic is modified.

Safety:
- compile before commit
- 10 combined unit tests
- direct dashboard API verify on port 8878
- commit/push only after all verification passes
