# Release Notes

V102.01–V103.00 adds the first real-time market-data processing layer.

The optional network runner requires explicit opt-in, credentials, an exact confirmation phrase, and the optional `websocket-client` package. It is not invoked by the standard test pipeline.
