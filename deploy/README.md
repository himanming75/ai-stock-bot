# Deployment Readiness

These deployment files are templates only.

Do not expose the service publicly until HTTPS, CSRF protection, rate limiting,
secret management, database migration, backup restore testing, and an
independent security review are complete.
